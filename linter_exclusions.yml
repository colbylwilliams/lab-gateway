lab-gateway create:
  parameters:
    appgateway_subnet_address_prefix:
      rule_exclusions:
        - option_length_too_long
    bastion_subnet_address_prefix:
      rule_exclusions:
        - option_length_too_long
    rdgateway_subnet_address_prefix:
      rule_exclusions:
        - option_length_too_long
